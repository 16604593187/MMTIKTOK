$kind: http-request
name: 'Favorite list'
method: GET
url: '{{base_url}}/douyin/favorite/list'
order: 9000
queryParams:
  - key: token
    value: '{{token}}'
    description: 用户鉴权 token
  - key: user_id
    value: '{{user_id}}'
    description: 用户 id
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status code is 200', function () {
        pm.response.to.have.status(200);
      });
